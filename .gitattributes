# Shell scripts must keep LF endings — CRLF makes bash (Linux / git-bash)
# fail with `$'\r': command not found`. core.autocrlf=true on Windows would
# otherwise convert them to CRLF on checkout.
*.sh text eol=lf
